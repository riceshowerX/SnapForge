# SnapForge npm 配置
# 使用官方 registry 以获取最新的安全更新
registry=https://registry.npmjs.org/

# 安全配置
# 禁止使用不安全的包
engine-strict=true

# 锁文件设置
lockfile-format=v6

# 忽略对等依赖冲突（某些安全覆盖可能需要）
auto-install-peers=true

# 严格检查依赖版本
strict-peer-dependencies=false
